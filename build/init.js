var Init = /** @class */ (function () {
    function Init() {
    }
    Init.start = function () {
        console.log('app running... !!!');
    };
    return Init;
}());
// start wep API
Init.start();
